TODOs:

- add server for configuration which allow subscription / stop
- create a little cellarctl tool
- resiliance to mongoDB disconnect + queue?
- push error message onto pipe whenever an action fail

CHANGELOG v0.1.1:

